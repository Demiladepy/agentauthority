/**
 * Policy Engine Tests
 *
 * Uses Node.js built-in test runner (node:test + node:assert).
 * Each test creates its own isolated engine and manager.
 *
 * Run: npm test
 */
export {};
//# sourceMappingURL=policy-engine.test.d.ts.map